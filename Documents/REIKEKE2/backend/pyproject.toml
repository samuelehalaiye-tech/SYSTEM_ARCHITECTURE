[project]
name = "reikeke-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # Optional: You can move your requirements.txt dependencies here later
]

[tool.vercel.scripts]
build = "python manage.py migrate --noinput && python manage.py collectstatic --noinput --clear"

[tool.vercel]
# Tell Vercel exactly where to find your WSGI app
entrypoint = "backend.wsgi:application"